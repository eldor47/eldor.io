# Assets

Images

- `public/images/*.png` used for project cards:
  - `topblast.png`, `woofytracker.png`, `pve.png`, `combo.png`, `baecafe.png`, `wavenest.png`

Fonts

- `public/fonts/RedHatDisplay.ttf` loaded via `next/font/local` in `_app.tsx` and applied as CSS variable.

Notes

- Optimize images where possible; consider Next/Image if moving to the app router or enabling in pages.
