# Tasks / Backlog

Near-term

- Add alt text review for images; ensure descriptive labels.
- Add page metadata component for title/description/social preview.
- Convert project images to Next/Image for optimization (if staying on pages router, enable it).

Housekeeping

- Consider switching to Next/Image for project images.
- Add a simple CI step for `npm run lint`.
- Pin Node version via `.nvmrc` or `engines` in `package.json`.

Future

- Extract projects list to a data file (TS/JSON) or CMS.
- Add tests (basic rendering smoke tests).
- Deploy guide (Vercel) and env notes.
